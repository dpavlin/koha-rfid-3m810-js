{
	"name": "koha-rfid-3m810-js",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "3M 810 RFID reader driven straight from Koha staff pages over Web Serial — no local server",
	"scripts": {
		"build": "node build/bundle.mjs",
		"test": "node --test tests/*.test.mjs"
	},
	"devDependencies": {
		"esbuild": "^0.25.0"
	}
}
